# =============================================================================
# PollDriver — plantilla → copiar a apps/admin-web/.env.local
# =============================================================================

VITE_SUPABASE_URL=https://TU_PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=tu_anon_key

VITE_APP_NAME=PollDriver
VITE_EL_POLLON_URL=https://el-pollon.cl

# MapLibre + OpenFreeMap (calles con nombres) — SIN MapTiler
VITE_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty

# OSRM (ruta / km / ETA). Default público si se omite.
# VITE_OSRM_BASE_URL=https://router.project-osrm.org
